# 协作约定（给 AI 助手）

本文件是**硬规则**，优先级高于「把活干完」的直觉。违反它的改动一律算返工。

## 1. 默认只讨论，不动代码

只要用户的话属于**讨论**——出现问号、「是否 / 建议 / 讨论 / 合理吗 / 我在想」，
或者用户在回答里反过来提问——就只回答 + 给方案，**不修改任何文件**。

只有明确指令才算授权：`开始改` / `按 A 改` / `执行` / `可以动手`。
「应该……」「这不太合理」这类方向性表述**不是**授权。

## 2. 动手前必须弹窗确认

即使我判断「现在可以开始了」，也必须先用提问工具（`ask_user_question`）让用户点头，
拿到明确选择后才动第一个文件。

不要用「落地范围」这类**已经预设了执行**的问题框架 —— 第一问应当是
「要不要现在动手」，而不是「按哪个范围动手」。

## 3. 方案怎么写

格式固定：**选项 → 推荐 → 影响面（文件清单、是否外部可见 API）→ 回退成本**，
结尾一句「等你确认」。一次只问关键决策，不要一口气抛一堆问题。

## 4. 只改被批准的范围

- 连带改动（改名、顺手统一措辞、顺手重构、顺手更新示例/文档）必须**单独再问一次**；
- 涉及 ≥3 个文件或外部可见 API 时，动手前先把「将要改的文件 + 一句话说明」贴给用户过目；
- 用户没提的地方不要「顺便」动。

## 5. 变更前的安全网

动手前把当前状态存成快照（`tar` 到 `/tmp`，要包含未跟踪的新文件；必要时再存一份 patch），
并在回复里给出路径，保证回退能一步完成。

## 6. 汇报

每轮结束时说清三件事：**改了什么**、**验证命令与结果**、**还有什么未决/没做**。
没验证过的事不要写成已验证；测试没跑就说没跑。

## 7. 文档与代码同等对待

文档、示例、类型声明、前端补全里的硬编码名字，都算「改动范围」的一部分，
同样受第 4 条约束。
