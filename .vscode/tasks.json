{
  // See https://go.microsoft.com/fwlink/?LinkId=733558
  // for the documentation about the tasks.json format
  "version": "2.0.0",
  "tasks": [
    {
      "label": "ui:dev",
      "type": "shell",
      "isBackground": true,
      "command": "pnpm",
      "options": {
        "cwd": "${workspaceFolder}"
      },
      "args": [
        "dev"
      ]
    },
    {
      "label": "ui:build",
      "type": "shell",
      "command": "pnpm",
      "options": {
        "cwd": "${workspaceFolder}"
      },
      "args": [
        "build"
      ]
    }
  ]
}
